name: C/Fortran Tests

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false # Prevents cancelling other OS jobs if one fails
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    # --- 1. Linux Dependencies ---
    - name: Install dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake gcc g++ gfortran ninja-build

    # --- 2. macOS Dependencies ---
    - name: Install dependencies (macOS)
      if: runner.os == 'macOS'
      run: brew install gcc ninja

    # --- 3. Windows Dependencies (MSYS2 MinGW) ---
    - name: Setup MSYS2 (Windows)
      if: runner.os == 'Windows'
      uses: msys2/setup-msys2@v2
      with:
        msystem: MINGW64
        update: true
        install: >-
          mingw-w64-x86_64-gcc
          mingw-w64-x86_64-gcc-fortran
          mingw-w64-x86_64-cmake
          mingw-w64-x86_64-ninja

    # --- 4. Build and Test ---
    # We use the msys2 shell on Windows to access GCC/gfortran, and standard bash on Linux/macOS
    - name: Configure CMake
      shell: ${{ runner.os == 'Windows' && 'msys2 {0}' || 'bash' }}
      run: |
        cmake -B build -S . \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_WINDOWS_EXPORT_ALL_SYMBOLS=TRUE

    - name: Build Stub
      shell: ${{ runner.os == 'Windows' && 'msys2 {0}' || 'bash' }}
      run: cmake --build build

    - name: Run Tests
      working-directory: build
      shell: ${{ runner.os == 'Windows' && 'msys2 {0}' || 'bash' }}
      run: ctest --output-on-failure